umask 0077

if [ ! -d ~/.secrets ]; then
    mkdir ~/.secrets
fi

if [ ! -d ~/.secrets.locked ]; then
    mkdir ~/.secrets.locked
fi

# has_local_secret() { [[ -e ~/.secrets/"$1" || -e ~/.secrets.locked/"$1" ]] }

# vim: ft=zsh
